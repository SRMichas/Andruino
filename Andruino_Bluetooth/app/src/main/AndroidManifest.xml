<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.sorezel.andruino_bluetooth">
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_invernadero"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".v1.MainActivity">

        </activity>
        <activity android:name=".v1.BluetoothActivity">

        </activity>
        <activity android:name=".v1.BluetoothActivity2"/>
        <activity android:name=".invernadero.StartScreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".invernadero.MainActivity"/>
        <activity android:name=".invernadero.InvernaderoActivity"/>
        <activity android:name=".omar.ScanActivity">

        </activity>
        <activity android:name=".omar.ChatActivity"/>
        <activity android:name=".invernadero.testActivity">

        </activity>
    </application>

</manifest>